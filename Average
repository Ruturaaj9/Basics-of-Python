def average(array):
    heights = set(array)
    averg = sum(heights) / len(heights)
    return averg
if __name__ == '__main__':
    n = int(raw_input())
    arr = map(int, raw_input().split())
    result = average(arr)
    print result
