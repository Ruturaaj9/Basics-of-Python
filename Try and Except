try:
    hours = raw_input("Enter number of hours: ")
    hrs = float(hours)
    rate = raw_input("Enter rate: ")
    r = float(rate)
    if hrs <= 40:
        pay = hrs * r
    else:
        extra_hours = hrs - 40
        pay = 40 * r + extra_hours * r * 1.5
    print pay
except:
    print "Error, please enter numeric input"
